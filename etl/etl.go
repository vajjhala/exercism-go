//Package etl to transform scores
package etl

import ("strings")

out := make(map[string]int)

func load(vals []string) {
    for _,v := range vals {
        
    }
}

//Transform (map[int][]string) map[string]int
func Transform(in map[int][]string) map[string]int {
	for k, v := range in {

	}
}
